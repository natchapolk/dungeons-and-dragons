# CLAUDE.md

คู่มือสำหรับ Claude Code เมื่อทำงานกับ repository นี้

---

## โปรเจกต์นี้คืออะไร

**คู่มือ Dungeons & Dragons ภาษาไทยสำหรับผู้เล่นใหม่** อ้างอิงกฎ **D&D 2024 Player's Handbook** (ไม่ใช่กฎ 2014)

เป็น repository ของเอกสาร Markdown ล้วน — ไม่มีโค้ด ไม่มี build step ไม่มี dependency

---

## กฎการเขียน (สำคัญที่สุด — ห้ามผิดจากนี้)

### 1. รูปแบบคำศัพท์: ไทย (English)

**ทุกครั้งที่พูดถึงชื่อเฉพาะในเกม ให้เขียนคำไทยก่อน แล้ววงเล็บภาษาอังกฤษต่อท้าย**

| ถูก | ผิด |
|---|---|
| `**โหมดคลั่ง (Rage)**` | `**Rage (โหมดคลั่ง)**` |
| `**การโจมตีลับ (Sneak Attack)**` | `**Sneak Attack** (การโจมตีลับ)` |
| `**ล้มลงนอน (Prone)**` | `**Prone** (ล้มลงนอน)` |
| `# นักรบคลั่ง (Barbarian)` | `# Barbarian — นักรบคลั่ง` |
| `**ลูกไฟ (Fireball)**` | `` `Fireball` (ลูกไฟ) `` |

**ข้อยกเว้น — คงภาษาอังกฤษล้วนได้ เมื่อ:**
- เป็นคำที่ไม่มีคำแปลไทยที่ใช้กันจริง และผู้เล่นไทยเรียกทับศัพท์อยู่แล้ว
  เช่น `HP`, `AC`, `DC`, `Spell Slot`, `Saving Throw`, `Proficiency Bonus`, `Advantage`, `Bonus Action`, `Short Rest`, `Long Rest`, `Concentration`, `Cantrip`
- อยู่ในตารางที่ทั้งคอลัมน์เป็นค่าตัวเลขหรือตัวย่อ เช่น `STR`, `d12`, `1d8 + 4`
- เป็น **ชื่อสเปลในรายการอ้างอิงย่อ** ที่ปรากฏซ้ำหลายที่ (ใช้ backtick: `` `Fireball` ``)

> **หลักตัดสิน:** ถ้าเป็น "ชื่อของสิ่งหนึ่ง" ในเกม ต้องมีคำไทยนำหน้า
> ถ้าเป็น "ค่า / หน่วย / ตัวย่อ" ปล่อยเป็นอังกฤษได้

### 2. ห้ามใช้อีโมจิและสัญลักษณ์ตกแต่ง

เอกสารทั้งชุด **ไม่มีอีโมจิเลย** ห้ามเพิ่มกลับเข้าไป

| ห้ามใช้ | ใช้แทนด้วย |
|---|---|
| อีโมจิทุกชนิด | ไม่ใส่อะไรเลย |
| ดาวแสดงความแนะนำ | `[แนะนำ]` / `[แนะนำมาก]` / `[แนะนำอย่างยิ่ง]` ต่อท้ายชื่อ |
| เครื่องหมายถูกและผิด | คำว่า "ใช่" / "ไม่" หรือเขียนอธิบายตรง ๆ |
| ลูกศร | เว้นวรรค หรือใช้คำเชื่อมไทย ("แล้ว", "จะ", "กลายเป็น") |
| หัวข้อนำด้วยอีโมจิ | หัวข้อข้อความล้วน |

**เครื่องหมายที่ใช้ได้:** `—` (em dash), `•` (bullet ในเซลล์ตาราง), `=`, `+`, `×`, `/`

### 3. กลุ่มเป้าหมาย

เขียนให้ **คนที่ไม่เคยเล่น D&D มาก่อนเลย** อ่านเข้าใจ:
- อธิบายศัพท์ก่อนใช้เสมอ
- ใส่ตัวอย่างการคำนวณจริง (ตัวเลขจริง)
- ทุกไฟล์คลาสต้องมีหัวข้อ **"ข้อผิดพลาดที่มือใหม่ทำบ่อย"**
- ทุกครั้งที่มีตัวเลือกหลายทาง ต้องบอกว่าอันไหนแนะนำสำหรับมือใหม่

### 4. รูปแบบที่ใช้เหมือนกันทุกไฟล์

- เปิดไฟล์ด้วยลิงก์กลับ: `[กลับหน้ารวม...](...)` และ `[สารบัญ](../README.md)`
- ใช้ **ตาราง Markdown** เป็นหลัก อ่านง่ายกว่าย่อหน้ายาว
- เส้นคั่นตารางเขียนติดกัน: `|---|---|` (ไม่ใช่ `| --- | --- |`)
- ปิดไฟล์ด้วยหัวข้อ **"สรุป ... ในหนึ่งบรรทัด"** แล้วตามด้วยลิงก์กลับ
- ระบุให้ชัดตรงจุดที่ **กฎ 2024 เปลี่ยนจากกฎ 2014**
- ตารางสเปลใช้ `[C]` = Concentration, `[R]` = Ritual
- **ห้ามใช้ลิงก์สมอในหน้า** (`](#หัวข้อ)`) เพราะหัวข้อเป็นภาษาไทยทำให้ anchor เพี้ยน ให้ใช้ตัวหนาแทน

### 5. โครงสร้างไฟล์คลาส (ต้องครบทุกหัวข้อตามลำดับนี้)

```
1. ภาพรวม
2. ข้อมูลพื้นฐาน (Class Table) + อุปกรณ์เริ่มต้น
3. ตารางความก้าวหน้าเลเวล 1-20 (รวม Spell Slot ถ้าเป็นสายเวท)
4. Features ทีละเลเวล (รายละเอียด)
5. Subclasses ทั้ง 4 สาย (Feature ทุกเลเวลที่ได้)
6. ตารางเปรียบเทียบ 4 Subclass
7. รายการเวททั้งหมดแยกตามระดับ (เฉพาะสายเวท) Cantrip ถึงระดับสูงสุด
8. คำแนะนำการสร้าง (ค่าพลัง / Species / Background / Feat)
9. ข้อผิดพลาดที่มือใหม่ทำบ่อย
10. สรุปในหนึ่งบรรทัด
```

### 6. ความถูกต้องของกฎ

- ยึด **PHB 2024** เท่านั้น ห้ามผสมกฎ 2014
- ถ้าไม่แน่ใจรายละเอียดกฎ **ให้ระบุว่าควรเช็คกับหนังสือต้นฉบับ** อย่าเดาแล้วเขียนให้ดูมั่นใจ
- คำอธิบาย Feature และสเปลเป็น **การย่อ ไม่ใช่การแปลคำต่อคำ**

---

## โครงสร้างไฟล์

```
README.md สารบัญกลาง (ต้องอัปเดตทุกครั้งที่เพิ่มไฟล์)
CLAUDE.md ไฟล์นี้

01-basics/ กฎพื้นฐาน 10 ไฟล์
02-species/ เผ่าพันธุ์ (overview + 10 เผ่า)
03-backgrounds/ ภูมิหลัง 16 แบบ + Origin Feats
04-classes/ คลาส (overview + 12 คลาส)
05-feats/ Feats (overview + 3 ไฟล์)
06-dm/ คู่มือผู้คุมเกม (overview + 5 ไฟล์)
```

---

## สถานะ: เสร็จสมบูรณ์ (อัปเดต 2026-08-08)

**71 ไฟล์ ประมาณ 22,600 บรรทัด — ครบทุกอย่างที่ต้องใช้ในการเล่นจริง**

| โฟลเดอร์ | จำนวน | ไฟล์ |
|---|---|---|
| root | 2 | `README.md` · `CLAUDE.md` |
| **01-basics/** | **11/11** | `01-what-is-dnd` · `02-dice-and-rolls` · `03-ability-scores` · `04-character-creation` · `05-combat` · `06-conditions` · `07-spellcasting-rules` · `08-equipment` · `09-rest-and-leveling` · `10-adventuring` · `11-multiclassing` |
| **02-species/** | **11/11** | `00-species-overview` + Aasimar · Dragonborn · Dwarf · Elf · Gnome · Goliath · Halfling · Human · Orc · Tiefling |
| **03-backgrounds/** | **2/2** | `00-backgrounds-overview` (16 แบบ) · `origin-feats` (10 Feats) |
| **04-classes/** | **13/13** | `00-classes-overview` + Barbarian · Bard · Cleric · Druid · Fighter · Monk · Paladin · Ranger · Rogue · Sorcerer · Warlock · Wizard |
| **05-feats/** | **4/4** | `00-feats-overview` · `general-feats` · `fighting-styles` · `epic-boons` |
| **06-dm/** | **6/6** | `00-dm-overview` · `01-running-the-game` · `02-building-encounters` · `03-monsters` · `04-magic-items` · `05-adventure-design` |
| **07-pregen/** | **9/9** | `00-pregen-overview` + ตัวละครเลเวล 1 พร้อมเล่น 8 ตัว (ครบทุกบทบาทในทีม) |
| **08-bestiary/** | **4/4** | `00-bestiary-overview` · `01-tier-1` (17 ตัว) · `02-tier-2` (11 ตัว) · `03-tier-3-4` (5 บอส) |
| **09-adventures/** | **2/2** | `00-adventures-overview` · `01-blackreach-mine` (เลเวล 1-3, 2-3 เซสชัน) |
| **10-spells/** | **7/7** | `00-spells-overview` · `01-cantrips` ถึง `06-level-6-9` (ประมาณ 140 สเปล) |

**ตรวจแล้ว:** ลิงก์ภายในใช้ได้ทุกอัน · โครงสร้างตารางถูกต้องทุกตาราง · ไม่มีอีโมจิเหลืออยู่

---

## งานที่อาจทำต่อได้ (ยังไม่ได้ทำ)

| หัวข้อ | หมายเหตุ |
|---|---|
| สเปลที่เหลือใน PHB | `10-spells/` มีประมาณ 140 สเปลที่ใช้บ่อยที่สุด ยังไม่ครบทุกอันในหนังสือ |
| มอนสเตอร์เพิ่มเติม | `08-bestiary/` มี 33 ตัว ยังไม่ครบ Monster Manual |
| การผจญภัยเรื่องที่ 2 ขึ้นไป | ตอนนี้มี 1 เรื่อง (เลเวล 1-3) ยังไม่มีเรื่องต่อสำหรับเลเวล 4 ขึ้นไป |
| ตัวละครสำเร็จรูปเลเวลสูง | ตอนนี้มีแค่เลเวล 1 (8 ตัว) |
| ของวิเศษแบบมี Stat Block เต็ม | `06-dm/04-magic-items.md` เป็นตารางสรุป ยังไม่มีรายละเอียดเต็มแต่ละชิ้น |

---

## คำสั่งที่ใช้บ่อย

```bash
# ดูไฟล์ทั้งหมด
find . -name "*.md" | sort

# นับบรรทัดรวม
wc -l $(find . -name "*.md") | tail -1
```

ตรวจว่ามีอีโมจิหลุดเข้ามาไหม:

```bash
python3 -c "
import pathlib
for f in pathlib.Path('.').rglob('*.md'):
    for ch in f.read_text(encoding='utf-8'):
        o = ord(ch)
        if 0x1F000 <= o <= 0x1FAFF or 0x2600 <= o <= 0x27BF or 0x2B00 <= o <= 0x2BFF:
            print(f, repr(ch)); break
"
```

ตรวจลิงก์เสีย:

```bash
python3 -c "
import pathlib, re
for f in pathlib.Path('.').rglob('*.md'):
    for m in re.finditer(r'\]\(([^)#][^)]*[.]md)\)', f.read_text(encoding='utf-8')):
        if not (f.parent / m.group(1)).resolve().exists(): print(f, m.group(1))
"
```

---

## สิ่งที่ต้องระวัง

1. **อัปเดต `README.md` ทุกครั้งที่เพิ่มไฟล์** — ตารางสารบัญอยู่ที่นั่น
2. **ตรวจลิงก์สัมพัทธ์** — ไฟล์ในโฟลเดอร์ย่อยใช้ `../README.md` ส่วนไฟล์ในโฟลเดอร์เดียวกันใช้ชื่อไฟล์ตรง ๆ
3. **อย่าเปลี่ยนรูปแบบกลางคัน** — 71 ไฟล์ใช้รูปแบบเดียวกันหมด ไฟล์ใหม่ต้องตามให้ตรง
4. **ไฟล์คลาสสายเวทยาว 600-800 บรรทัด** เพราะต้องมีรายการเวทครบทุกระดับ ถือว่าปกติ
5. **ห้ามแก้เอกสารด้วยสคริปต์ regex ทั้ง repo โดยไม่ทดสอบกับสำเนาก่อน** — เคยทำ blockquote และตัวหนาซ้อนพังมาแล้ว ให้ copy ไปทดสอบใน temp directory เสมอ และระวัง `\s` ใน regex ที่ข้ามบรรทัดได้
6. เอกสารเป็น **สรุปเพื่อการเรียนรู้ ไม่ใช่การแปลหนังสือ** — disclaimer ลิขสิทธิ์อยู่ท้าย `README.md`
