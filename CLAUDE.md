# CLAUDE.md

คู่มือสำหรับ Claude Code เมื่อทำงานกับ repository นี้

---

## โปรเจกต์นี้คืออะไร

คู่มือ Dungeons and Dragons ภาษาไทยสำหรับผู้เล่นใหม่ อ้างอิงกฎ D&D 2024 Player's Handbook (ไม่ใช่กฎ 2014)

เป็น repository ของเอกสาร Markdown ล้วน ไม่มีโค้ด ไม่มี build step ไม่มี dependency

**ผู้อ่านหลักคนหนึ่งใช้ screen reader** ดังนั้นตั้งแต่เวอร์ชัน 2 เป็นต้นไป เอกสารทุกไฟล์ต้องเขียนแบบ "ข้อความก่อน ตารางทีหลัง" ดูกฎข้อ 2 และ 3

---

## กฎการเขียน (สำคัญที่สุด ห้ามผิดจากนี้)

### 1. รูปแบบคำศัพท์ ไทย (English)

ทุกครั้งที่พูดถึงชื่อเฉพาะในเกม ให้เขียนคำไทยก่อน แล้ววงเล็บภาษาอังกฤษต่อท้าย

เขียนถูก

- `**โหมดคลั่ง (Rage)**`
- `**การโจมตีลับ (Sneak Attack)**`
- `**ล้มลงนอน (Prone)**`
- `# นักรบคลั่ง (Barbarian)`
- `**ลูกไฟ (Fireball)**`

เขียนผิด

- `**Rage (โหมดคลั่ง)**` เพราะเอาอังกฤษขึ้นก่อน
- `**Sneak Attack** (การโจมตีลับ)` เพราะตัวหนาครอบไม่ครบ
- `# Barbarian — นักรบคลั่ง` เพราะใช้ em dash และเรียงผิด
- `` `Fireball` (ลูกไฟ) `` เพราะใช้ backtick และเรียงผิด

คงภาษาอังกฤษล้วนได้ เมื่อเป็นคำที่ผู้เล่นไทยเรียกทับศัพท์อยู่แล้ว เช่น HP, AC, DC, Spell Slot, Saving Throw, Proficiency Bonus, Advantage, Disadvantage, Bonus Action, Reaction, Short Rest, Long Rest, Concentration, Cantrip หรือเมื่อเป็นค่าตัวเลขและตัวย่อ เช่น STR, d12, 1d8 + 4

หลักตัดสิน ถ้าเป็น "ชื่อของสิ่งหนึ่ง" ในเกม ต้องมีคำไทยนำหน้า ถ้าเป็น "ค่า หน่วย หรือตัวย่อ" ปล่อยเป็นอังกฤษได้

### 2. เขียนเป็นข้อความ ไม่ใช่ตาราง (กฎใหม่ สำคัญที่สุด)

screen reader อ่านตารางโดยประกาศชื่อคอลัมน์ซ้ำทุกช่อง ตารางที่ยาวหรือกว้างจึงฟังแล้วจับใจความไม่ได้เลย

**เก็บไว้เป็นตารางได้ ก็ต่อเมื่อครบทั้ง 4 ข้อนี้**

1. เป็นการ "เทียบ" ของหลายอย่างด้วยหัวข้อชุดเดียวกันจริง ๆ
2. ไม่เกิน 4 คอลัมน์
3. ไม่เกิน 12 แถว
4. ทุกช่องสั้น ประมาณไม่เกิน 8 คำ ไม่มีประโยคซ้อนประโยค ไม่มีตัวหนาซ้อนในช่อง

ถ้าขาดข้อใดข้อหนึ่ง ให้เขียนเป็นข้อความตามรูปแบบในกฎข้อ 3

**ตารางที่เก็บไว้ทุกตาราง ต้องมีย่อหน้านำหน้า** บอกว่าตารางนี้เทียบอะไร มีคอลัมน์อะไรบ้าง และข้อสรุปที่ได้จากตารางคืออะไร เพื่อให้คนที่ข้ามตารางไปยังได้ใจความครบ

**ตารางที่ต้องแปลงเป็นข้อความเสมอ** ไม่มีข้อยกเว้น

- รายละเอียดสเปลทุกอัน (ตาราง Casting Time, Range, Components, Duration)
- รายการสเปลของคลาส
- ตารางข้อมูลพื้นฐานคลาส (Class Table)
- ตารางความก้าวหน้าเลเวล 1 ถึง 20
- ตารางคุณสมบัติเผ่าพันธุ์ ภูมิหลัง Feat และ Fighting Style
- Stat Block มอนสเตอร์
- ตารางสภาวะผิดปกติ (Conditions)
- ตารางสกิลทั้ง 18 อย่าง
- ตารางที่คอลัมน์แรกว่างเปล่า (ตารางแบบ key และ value)

### 3. รูปแบบข้อความที่ใช้แทนตาราง (มี 3 แบบ ใช้ให้ตรงงาน)

**แบบ ก รายการหัวข้อและค่า** ใช้แทนตาราง 2 คอลัมน์แบบ key และ value

```
- ค่าพลังหลัก (Primary Ability) คือ Intelligence
- Hit Die คือ d6 ซึ่งน้อยที่สุดในเกม
- HP ที่เลเวล 1 คือ 6 บวกกับ CON modifier
- Saving Throw ที่ถนัด คือ Intelligence และ Wisdom
- ความชำนาญเกราะ ไม่มีเลย
```

**แบบ ข หัวข้อย่อยต่อหนึ่งรายการ** ใช้แทนตารางรายการยาว เช่น สเปล เผ่าพันธุ์ Feat มอนสเตอร์ สภาวะผิดปกติ
ข้อดีคือ screen reader กระโดดข้ามหัวข้อได้ ทำให้หาของที่ต้องการเจอเร็ว

```
### ลูกไฟ (Fire Bolt)

เวทระดับ 0 สำนัก Evocation ใช้ได้โดยคลาส Sorcerer และ Wizard

- ร่ายด้วย Action
- ระยะ 120 ฟุต
- องค์ประกอบ V และ S
- คงอยู่ ทันที

ยิงลูกไฟใส่เป้าหมาย ทอย Spell Attack ถ้าโดนทำดาเมจ 1d10 Fire
วัตถุที่ไม่มีใครถือหรือสวมใส่จะติดไฟถ้าโดน

แนะนำ เพราะเป็น Cantrip โจมตีที่ดาเมจสูงที่สุดและระยะไกลที่สุด
เป็นการโจมตีหลักของ Wizard และ Sorcerer
```

**แบบ ค หนึ่งบรรทัดต่อหนึ่งเลเวล** ใช้แทนตารางความก้าวหน้าเลเวล 1 ถึง 20 ที่กว้างถึง 14 คอลัมน์

```
- เลเวล 1 ได้ Proficiency Bonus +2 ได้ความสามารถ Spellcasting, Ritual Adept และ Arcane Recovery
  รู้ Cantrip 3 อัน เตรียมสเปลได้ 4 อัน มี Spell Slot ระดับ 1 จำนวน 2 ช่อง
- เลเวล 2 ได้ Proficiency Bonus +2 ได้ความสามารถ Scholar
  รู้ Cantrip 3 อัน เตรียมสเปลได้ 5 อัน มี Spell Slot ระดับ 1 จำนวน 3 ช่อง
```

เลเวลที่ไม่ได้ความสามารถใหม่ ให้เขียนว่า "ไม่ได้ความสามารถใหม่" แทนการใส่ขีด

### 4. สัญลักษณ์ที่ห้ามใช้แล้ว

screen reader อ่านสัญลักษณ์ออกเสียงหรืออ่านข้ามแบบสับสน จึงต้องเลี่ยงให้มากที่สุด

ห้ามใช้ และให้ใช้อะไรแทน

- em dash `—` ห้ามใช้ ให้ขึ้นบรรทัดใหม่ หรือใช้คำเชื่อมไทย เช่น "คือ" "เพราะ" "แล้ว" "ได้แก่"
- en dash `–` ห้ามใช้ ให้ใช้คำว่า "ถึง" เช่น "เลเวล 1 ถึง 20"
- middot `·` ห้ามใช้ ให้ใช้คำว่า "และ" หรือขึ้นบรรทัดใหม่
- bullet `•` ห้ามใช้ ถ้าคั่นของ 2 อย่างให้ใช้ "และ" ถ้าคั่นของ 3 อย่างขึ้นไปให้ใช้จุลภาคคั่นกลาง แล้วใช้ "และ" ก่อนตัวสุดท้าย
  ข้อนี้เพิ่มในเวอร์ชัน 3 เพราะสคริปต์ตรวจเดิมตรวจแค่ `·` (U+00B7) จึงจับ `•` (U+2022) ไม่ได้ ตอนนั้นตกค้างอยู่ 221 จุดใน 24 ไฟล์
- `×` ห้ามใช้ ให้เขียน "2 อัน" หรือ "3 เท่า"
- `≠` ห้ามใช้ ให้เขียนว่า "ไม่ใช่"
- ลูกศรทุกแบบ ห้ามใช้ ให้ใช้คำว่า "แล้ว" "จะกลายเป็น"
- backtick รอบชื่อสเปล ห้ามใช้ ให้ใช้รูปแบบ ไทย (English) ตามกฎข้อ 1
- `[C]` และ `[R]` ห้ามใช้ ให้เขียนเต็มว่า "ต้องใช้สมาธิ (Concentration)" และ "ร่ายเป็นพิธีกรรมได้ (Ritual)"
- blockquote `>` ห้ามใช้ ให้เขียนเป็นย่อหน้าปกติ ขึ้นต้นด้วยคำบอกประเภท เช่น "แนะนำ" "ข้อควรระวัง" "ตัวอย่าง" "กฎ 2024 ที่เปลี่ยนไป"
- code fence สำหรับสูตรคำนวณ ห้ามใช้ ให้เขียนเป็นประโยคไทย เช่น "Modifier หาได้จาก เอาค่า Ability Score ลบ 10 แล้วหารด้วย 2 ปัดเศษลง"
- ตัวหนากลางประโยคเพื่อเน้นอารมณ์ ห้ามใช้ ให้ใช้ตัวหนาเฉพาะตอนเขียนชื่อของสิ่งหนึ่งในเกม หรือคำแรกของหัวข้อย่อยเท่านั้น
- อีโมจิทุกชนิด ห้ามใช้ เหมือนเดิม
- เครื่องหมายถูกและผิด ห้ามใช้ ให้เขียนคำว่า "ใช่" และ "ไม่" ตรง ๆ

สัญลักษณ์ที่ยังใช้ได้ คือ หัวข้อ `#`, bullet `-`, วงเล็บ, ตัวเลข, `+`, `/`, เครื่องหมายลบในค่าติดลบ, ตัวหนา `**` ตามเงื่อนไขข้างบน และเครื่องหมายวรรคตอนไทยปกติ

ระดับคำแนะนำยังใช้ `[แนะนำ]` `[แนะนำมาก]` `[แนะนำอย่างยิ่ง]` ต่อท้ายชื่อเหมือนเดิม เพราะอ่านออกเสียงแล้วเข้าใจได้

### 4.1 คำว่า "ตาราง" ใช้ได้เฉพาะตอนที่มีตารางจริง (กฎใหม่เวอร์ชัน 3)

เมื่อแปลงตารางเป็นข้อความแล้ว ข้อความรอบ ๆ ยังเรียกของสิ่งนั้นว่า "ตาราง" อยู่
screen reader จะประกาศว่ามีตาราง ทั้งที่ข้างล่างเป็นรายการ ทำให้ผู้ฟังสับสนและหาไม่เจอ

- ถ้าข้างล่างเป็น bullet list ให้เรียกว่า **"รายการ"** เช่น "รายการสรุปทั้ง 16 Background"
- ถ้าเป็นรายการที่ต้องทอยเต๋าเลือก ให้เรียกว่า **"รายการสุ่ม"**
- ถ้าหมายถึงความก้าวหน้าตามเลเวล ให้เรียกว่า **"ความก้าวหน้าเลเวล 1 ถึง 20"** ไปเลย
- คำว่า "ตาราง" เก็บไว้ใช้เฉพาะย่อหน้านำหน้าตารางจริงเท่านั้น ตามกฎข้อ 2

คำที่มี "ตาราง" ประกอบแต่ไม่ได้แปลว่าตาราง ต้องเปลี่ยนด้วย

- "แผนที่ตาราง" ให้เขียนว่า **"แผนที่สนามรบ (Battle Map)"**
- "ตารางเวลา" ที่แปลว่านัดหมาย ให้เขียนว่า **"รอบเวลาเล่น"** หรือ **"เวลาที่ทุกคนว่างตรงกัน"**
- "ตาราง N ฟุต" ที่แปลว่าพื้นที่สี่เหลี่ยม ให้เขียนว่า **"พื้นที่สี่เหลี่ยมจัตุรัส ด้านละ N ฟุต"**

### 4.2 ทุกรายการที่แปลงมาจากตาราง ต้องมีชื่อคอลัมน์กำกับในทุกบรรทัด (กฎใหม่เวอร์ชัน 3)

ตอนแปลงตารางด้วยสคริปต์ในเวอร์ชัน 2 ชื่อคอลัมน์หายไป เหลือแต่ค่าเรียงกันเฉย ๆ
เช่น `- 1 5` หรือ `- **d12** 12+CON STR STR, CON Light, Medium, Shield ไม่ ง่าย`
บรรทัดแบบนี้ screen reader อ่านออกมาเป็นเสียงตัวเลขลอย ๆ ไม่มีทางเดาได้ว่าแต่ละค่าคืออะไร

- ทุกค่าต้องมีคำกำกับว่าเป็นค่าอะไร เช่น "เลเวล 1 คลังพลังรักษามี 5 HP"
- ถ้าบรรทัดยาว ให้มีย่อหน้านำหน้าบอกว่าแต่ละบรรทัดเรียงค่าอะไรตามลำดับไหน
- ตารางที่มีคอลัมน์เป็นตัวเลือก เช่น Circle Spells ของ Druid ที่แยกตามภูมิประเทศ
  ให้แตกเป็นหัวข้อย่อยรายตัวเลือก แล้วไล่ทีละเลเวลข้างใน อย่ายัดทุกตัวเลือกไว้บรรทัดเดียว

### 4.3 ตัวหนาใช้ได้แค่ 2 กรณี ห้ามใช้เน้นอารมณ์ (กฎใหม่เวอร์ชัน 4)

กฎข้อ 4 เขียนไว้แล้วว่าห้ามใช้ตัวหนาเน้นอารมณ์ แต่ไม่มีสคริปต์ตรวจ เวอร์ชัน 3 จึงตกค้างอยู่ 4,549 จุดใน 73 ไฟล์
screen reader ที่เปิดโหมดอ่านรูปแบบข้อความ จะประกาศคำว่า "ตัวหนา" ทุกจุด ทำให้ฟังแล้วสะดุดตลอดทั้งไฟล์

ใช้ตัวหนาได้แค่ 2 กรณีนี้เท่านั้น

1. ชื่อของสิ่งหนึ่งในเกม ตามรูปแบบ ไทย (English) ในกฎข้อ 1 เช่น `**ลูกไฟ (Fireball)**` หรือชื่ออังกฤษที่เรียกทับศัพท์ เช่น `**Spell Attack**`
2. คำแรกของ bullet หรือหัวข้อย่อย และต้องสั้น ไม่เกินประมาณ 30 ตัวอักษร

ห้ามใส่ตัวหนาให้ของพวกนี้เด็ดขาด

- ค่าตัวเลข ลูกเต๋า ระยะ เช่น `**17**`, `**+5**`, `**2d6**`, `**30 ฟุต**`, `**1d10 Fire damage**`
- วลีไทยที่เน้นอารมณ์ เช่น `**หนีทันที**`, `**ไม่ให้แล้วในกฎ 2024**`, `**ครึ่งเดียว**`
- ทั้งประโยคที่ยาวเกิน 30 ตัวอักษร ถึงจะอยู่ต้นบรรทัดก็ตาม
- ช่องในตาราง ห้ามมีตัวหนาเลย ตามกฎข้อ 2 เงื่อนไขที่ 4

### 4.4 ลิงก์ต้องบอกได้ด้วยตัวเองว่าไปไหน (กฎใหม่เวอร์ชัน 4)

screen reader มีคำสั่ง "อ่านรายการลิงก์ทั้งหมดในหน้านี้" ซึ่งอ่านเฉพาะ "ข้อความลิงก์" ออกมา โดยไม่อ่านข้อความรอบข้าง
ถ้าข้อความลิงก์เป็นชื่อไฟล์ ผู้ฟังจะได้ยินว่า "ศูนย์หนึ่ง ขีด ว็อท ขีด อิส ขีด ดี เอ็น ดี จุด เอ็ม ดี" ซึ่งเดาไม่ออกเลยว่าคืออะไร

- ข้อความลิงก์ต้องเป็นชื่อไทยที่มีความหมาย เขียนถูกคือ `[ค่าพลังและสกิล](01-basics/03-ability-scores.md)`
  เขียนผิดคือ `[03-ability-scores.md](01-basics/03-ability-scores.md)`
- ห้ามใช้ข้อความลิงก์กำกวม เช่น "ที่นี่" "คลิก" "ลิงก์"
- ห้ามใส่ลิงก์ไว้ในหัวข้อ `#` เพราะตอนนำทางด้วยหัวข้อ screen reader จะอ่านปนกับชื่อหัวข้อ
  ให้เขียนหัวข้อเป็นข้อความล้วน แล้วต่อบรรทัดใหม่ว่า "อ่านรายละเอียดเต็มได้ที่ [ชื่อ](ไฟล์.md)"

### 4.5 หัวข้อย่อยที่ซ้ำกัน ต้องเติมชื่อเจ้าของ (กฎใหม่เวอร์ชัน 4)

screen reader ใช้ "รายการหัวข้อ" เป็นเครื่องมือนำทางหลัก ถ้าไฟล์เดียวมีหัวข้อ `### Actions` เหมือนกัน 17 อัน
ผู้ฟังจะเห็นรายการที่อ่านว่า "Actions" 17 บรรทัดติดกัน แล้วเลือกไม่ถูกว่าอันไหนของมอนสเตอร์ตัวไหน

- หัวข้อย่อยทุกอันต้องมีชื่อเจ้าของกำกับ เช่น `### การกระทำ (Actions) ของโจร` ไม่ใช่ `### Actions`
- ห้ามมีหัวข้อชื่อซ้ำกันเกิน 3 อันในไฟล์เดียว
- ถ้าหัวข้อย่อยเป็นของหัวข้อไหน ต้องอยู่ระดับที่ลึกกว่าหัวข้อนั้น 1 ระดับ อย่าเขียนเป็นหัวข้อระดับเดียวกัน

### 5. กลุ่มเป้าหมาย

เขียนให้คนที่ไม่เคยเล่น D&D มาก่อนเลยอ่านเข้าใจ

- อธิบายศัพท์ก่อนใช้เสมอ
- ใส่ตัวอย่างการคำนวณจริงด้วยตัวเลขจริง
- ทุกไฟล์คลาสต้องมีหัวข้อ "ข้อผิดพลาดที่มือใหม่ทำบ่อย"
- ทุกครั้งที่มีตัวเลือกหลายทาง ต้องบอกว่าอันไหนแนะนำสำหรับมือใหม่

### 6. รูปแบบที่ใช้เหมือนกันทุกไฟล์

- เปิดไฟล์ด้วยลิงก์กลับ `[กลับหน้ารวม...](...)` และ `[สารบัญ](../README.md)`
- เส้นคั่นตารางเขียนติดกัน `|---|---|` ไม่ใช่ `| --- | --- |`
- ปิดไฟล์ด้วยหัวข้อ "สรุป ... ในหนึ่งบรรทัด" แล้วตามด้วยลิงก์กลับ
- ระบุให้ชัดตรงจุดที่กฎ 2024 เปลี่ยนจากกฎ 2014
- ห้ามใช้ลิงก์สมอในหน้า `](#หัวข้อ)` เพราะหัวข้อเป็นภาษาไทยทำให้ anchor เพี้ยน ให้อ้างชื่อหัวข้อเป็นข้อความแทน
- หัวข้อต้องเรียงระดับต่อเนื่อง ห้ามข้ามจาก `##` ไป `####` เพราะ screen reader ใช้ระดับหัวข้อในการนำทาง

### 7. โครงสร้างไฟล์คลาส (ปรับใหม่ในเวอร์ชัน 2)

```
1. ภาพรวม
2. ข้อมูลพื้นฐาน เขียนแบบ ก บวกอุปกรณ์เริ่มต้น
3. ความก้าวหน้าเลเวล 1 ถึง 20 เขียนแบบ ค
4. Features ทีละเลเวล
5. Subclasses ทั้ง 4 สาย
6. เปรียบเทียบ 4 Subclass (เก็บเป็นตารางได้ถ้าไม่เกิน 4 คอลัมน์ พร้อมย่อหน้าสรุปนำหน้า)
7. เวทของคลาสนี้ เขียนสรุปสั้น ๆ ว่าคลาสนี้ใช้เวทยังไง แล้วลิงก์ไป 10-spells/class-lists/ชื่อคลาส.md
8. คำแนะนำการสร้าง (ค่าพลัง Species Background Feat)
9. ข้อผิดพลาดที่มือใหม่ทำบ่อย
10. สรุปในหนึ่งบรรทัด
```

ข้อ 7 เปลี่ยนจากเดิม เดิมไฟล์คลาสมีตารางรายชื่อเวทยาวมาก ตอนนี้ย้ายไปไฟล์เวทต่อคลาสแล้ว

### 8. ความถูกต้องของกฎ

- ยึด PHB 2024 เท่านั้น ห้ามผสมกฎ 2014
- ถ้าไม่แน่ใจรายละเอียดกฎ ให้ระบุว่าควรเช็คกับหนังสือต้นฉบับ อย่าเดาแล้วเขียนให้ดูมั่นใจ
- คำอธิบาย Feature และสเปลเป็นการย่อ ไม่ใช่การแปลคำต่อคำ

---

## โครงสร้างไฟล์

```
README.md สารบัญกลาง (ต้องอัปเดตทุกครั้งที่เพิ่มไฟล์)
CLAUDE.md ไฟล์นี้

01-basics/ กฎพื้นฐาน 12 ไฟล์
02-species/ เผ่าพันธุ์ (overview บวก 10 เผ่า)
03-backgrounds/ ภูมิหลัง 16 แบบ บวก Origin Feats
04-classes/ คลาส (overview บวก 12 คลาส)
05-feats/ Feats (overview บวก 3 ไฟล์)
06-dm/ คู่มือผู้คุมเกม (overview บวก 5 ไฟล์)
07-pregen/ ตัวละครสำเร็จรูป (overview บวก 8 ตัว)
08-bestiary/ มอนสเตอร์ 4 ไฟล์
09-adventures/ การผจญภัย 2 ไฟล์
10-spells/ รายละเอียดสเปลเรียงตามระดับ 7 ไฟล์
10-spells/class-lists/ เวทแยกตามคลาส 9 ไฟล์ (สร้างใหม่ในเวอร์ชัน 2)
```

---

# งานเวอร์ชัน 5 เติมเนื้อหาไอเทมและอุปกรณ์เริ่มต้น (2026-08-16)

สถานะ **เสร็จแล้ว**

## ที่มาของงานนี้

เวอร์ชัน 1 ถึง 4 เน้นแก้รูปแบบให้ screen reader อ่านได้ ไม่ได้เติมเนื้อหาใหม่
พอมาดูหมวดไอเทม พบว่า `01-basics/08-equipment.md` มีของใช้ผจญภัยแค่ 20 ชิ้นจากที่ PHB 2024 มีประมาณ 100 ชิ้น
ชุดอุปกรณ์สำเร็จทั้ง 7 ชุดบอกแค่ราคา ไม่บอกว่าข้างในมีอะไร ทั้งที่อุปกรณ์เริ่มต้นของทุกคลาสอ้างถึงชุดพวกนี้
และไม่มีหัวข้อ Spellcasting Focus เลย ทั้งที่เป็นของที่สายเวททุกคลาสต้องมี
ส่วนอุปกรณ์เริ่มต้นของคลาส 13 ไฟล์ และของภูมิหลัง 16 แบบ เขียนเป็นอังกฤษดิบล้วน ผิดกฎข้อ 1 และไม่มีคำอธิบายว่าของแต่ละชิ้นใช้ทำอะไร

## สิ่งที่ทำ 5 เรื่อง

1. **สร้างไฟล์ใหม่ `01-basics/12-gear-and-items.md`** เก็บของที่ใช้ตอนผจญภัย
   ของใช้ผจญภัยประมาณ 100 ชิ้นจัดกลุ่มตามการใช้งาน 8 กลุ่ม (แสงสว่าง, ปีนป่าย, ภาชนะ, ของกินของใช้,
   ของใช้ตอนสู้, ของเกี่ยวกับเวทมนตร์, ของเขียน, ของใช้เฉพาะทาง),
   Spellcasting Focus ทั้ง 3 สายพร้อมกฎ 4 ข้อและองค์ประกอบเวท V, S, M,
   เครื่องมือทุกชนิดรวม 17 เครื่องมือช่าง 10 เครื่องดนตรี และ 4 ชุดเล่นเกม,
   ชุดอุปกรณ์สำเร็จทั้ง 7 ชุดพร้อมของข้างใน, ยา, น้ำหนักที่แบกได้,
   รายการของที่มือใหม่ควรซื้อก่อน และข้อผิดพลาดที่มือใหม่ทำบ่อย 7 ข้อ
2. **ย้ายหมวดของผจญภัยออกจาก `08-equipment.md`** ไฟล์นั้นเหลือเฉพาะของที่ใช้ตอนสู้
   คือเงิน, คุณสมบัติอาวุธ, Weapon Mastery, อาวุธ 38 ชนิด และเกราะ 15 ชนิด แล้วต่อลิงก์ไปไฟล์ใหม่
3. **เขียนอุปกรณ์เริ่มต้นของคลาสใหม่ทั้ง 13 ไฟล์** เปลี่ยนจากอังกฤษดิบเป็นรูปแบบ ไทย (English)
   แต่ละชิ้นบอกหน้าที่สั้น ๆ เช่นเกราะให้ AC เท่าไหร่ อาวุธดาเมจเท่าไหร่ ความชำนาญอะไร
   และปิดท้ายด้วยคำแนะนำว่ามือใหม่ควรเลือกตัวเลือกไหนเพราะอะไร ตามกฎข้อ 5
4. **เขียนอุปกรณ์ของภูมิหลังใหม่ทั้ง 16 แบบ** เปลี่ยนเป็นรูปแบบ ไทย (English)
   และเติมคำแนะนำว่าควรรับของหรือรับเงิน 50 GP แทน ซึ่งเดิมไม่มีบอกเลย
5. **แก้ข้อมูลกฎที่ผิด** เดิมเขียนว่า Monk ได้ **ความชำนาญอาวุธ (Weapon Mastery)**
   แต่ใน PHB 2024 มีแค่ 5 คลาสที่ได้ คือ Barbarian, Fighter, Paladin, Ranger และ Rogue
   Monk ใช้ **ศิลปะการต่อสู้ (Martial Arts)** ซึ่งเป็นคนละระบบ แก้ที่ `08-equipment.md` 3 จุด และเติมข้อควรระวังใน `monk.md`
   พร้อมกันนั้นแก้หัวข้อภูมิหลังทั้ง 16 อันที่เรียงอังกฤษก่อนไทย เช่นจาก `## 1. Acolyte นักบวชผู้ช่วย` เป็น `## 1. นักบวชผู้ช่วย (Acolyte)`

## เรื่องความถูกต้องของตัวเลข

ตามกฎข้อ 8 ของที่ยืนยันราคาหรือน้ำหนักในกฎ 2024 ไม่ได้ ไม่ได้เดาตัวเลขให้
แต่เขียนแค่ชื่อกับการใช้งาน แล้วรวมไว้ในหัวข้อ "ของที่ต้องเช็คกับหนังสือต้นฉบับ" ในไฟล์ใหม่
ได้แก่ ชอล์ก, ค้อน, สบู่, ครั่งปิดผนึก และแหวนตรา ซึ่งเป็นของที่มีในกฎ 2014 แต่ไม่แน่ใจว่ากฎ 2024 ยังมีอยู่ไหม
ส่วนของข้างในชุดอุปกรณ์สำเร็จ ใส่รายชื่อของไว้ครบ แต่กำกับไว้ว่าจำนวนของบางชิ้นให้เช็คกับหนังสือต้นฉบับก่อน

## เรื่องที่ตรวจแล้วไม่ต้องแก้

**Weapon Mastery ครบอยู่แล้ว** ตรวจแล้วว่า `08-equipment.md` มีครบทั้ง 8 คุณสมบัติ
และรายชื่ออาวุธของแต่ละคุณสมบัติรวมกันได้ 38 ชนิดพอดี ตรงกับจำนวนอาวุธทั้งหมดในเกม ไม่มีอาวุธไหนตกหล่น

**ข้อมูลที่ควรเช็คเพิ่มในอนาคต** ดาเมจของ **น้ำมนต์ (Holy Water)** ในไฟล์เดิมเขียนไว้ว่า 2d6
ซึ่งเป็นตัวเลขของกฎ 2014 กฎ 2024 น่าจะเปลี่ยนแล้ว ตอนนี้เขียนไว้ว่าให้เช็คกับหนังสือต้นฉบับ

---

# งานเวอร์ชัน 4 เก็บกฎที่ไม่มีสคริปต์ตรวจ (2026-08-16)

สถานะ **เสร็จแล้ว**

## ที่มาของงานนี้

เวอร์ชัน 3 บันทึกไว้ว่า "ผลตรวจหลังทำเสร็จ ทุกอย่างเป็น 0" ซึ่งจริงตามสคริปต์ที่มี
แต่พอตรวจกฎที่**ไม่มี**สคริปต์ กลับพบปัญหาค้างอยู่หลายพันจุด
สาเหตุคือสคริปต์ตรวจครอบคลุมแค่ 8 เรื่อง จากกฎประมาณ 20 เรื่อง
กฎข้อไหนมีสคริปต์ ผลเป็น 0 จริง กฎข้อไหนไม่มีสคริปต์ ไม่เคยมีใครตรวจเลยตั้งแต่เวอร์ชัน 2

## สิ่งที่แก้ 9 เรื่อง

1. **ตัวหนาเน้นอารมณ์ 4,549 จุด ใน 73 ไฟล์** ผิดกฎข้อ 4 มาตั้งแต่ต้น แต่ไม่มีสคริปต์จับ
   ลบตัวหนาออกจากค่าตัวเลข ลูกเต๋า ระยะ และวลีไทยที่เน้นอารมณ์
   เก็บตัวหนาไว้เฉพาะชื่อของสิ่งในเกม และคำแรกที่สั้นของ bullet เพิ่มเป็นกฎข้อ 4.3 แล้ว
2. **ข้อความลิงก์ที่เป็นชื่อไฟล์ดิบ 86 จุด** อยู่ในสารบัญกลางของ `README.md` ทั้ง 78 จุด
   บวก `07-pregen/00-pregen-overview.md` อีก 8 จุด
   เปลี่ยนเป็นชื่อไทยที่มีความหมาย เช่น จาก `[03-ability-scores.md](...)` เป็น `[ค่าพลังและสกิล](...)`
   เพิ่มเป็นกฎข้อ 4.4 แล้ว
3. **ลิงก์ที่ฝังในหัวข้อ 3 จุด** ใน `08-bestiary/00-bestiary-overview.md`
   ย้ายออกมาเป็นบรรทัด "อ่านรายละเอียดเต็มได้ที่ ..." ใต้หัวข้อแทน
4. **หัวข้อย่อยซ้ำกันจนนำทางไม่ได้ 198 จุด** เช่น `### Actions` 17 อันในไฟล์เดียว
   เติมชื่อเจ้าของให้ทุกอัน เช่น `### การกระทำ (Actions) ของโจร` เพิ่มเป็นกฎข้อ 4.5 แล้ว
   ส่วน `05-feats/general-feats.md` หัวข้อย่อยเคยอยู่ระดับเดียวกับชื่อ Feat ลดเป็นระดับ 4 ให้ซ้อนถูกต้องแล้ว
5. **หัวข้ออังกฤษล้วน 45 จุด** ผิดกฎข้อ 1 ส่วนใหญ่คือ `### Actions` และ `### Spellcasting` ใน `08-bestiary/`
6. **ตัวหนาในช่องตาราง 97 แถว ใน 33 ตาราง** ผิดกฎข้อ 2 เงื่อนไขที่ 4
   ตารางเทียบที่เข้าเงื่อนไขอื่นครบ ลบตัวหนาในช่องออกแล้วเก็บไว้เป็นตารางต่อได้
   ส่วนตารางค่าพลังและตารางทักษะของตัวละครสำเร็จรูปทั้ง 8 ตัว รวม 16 ตาราง เป็น Stat Block
   ซึ่งกฎข้อ 2 สั่งให้แปลงเป็นข้อความเสมอ แปลงเป็นแบบ ก แล้ว ตอนนี้เหลือตาราง 23 ตาราง
7. **รายการที่ชื่อคอลัมน์หายไป** ผิดกฎข้อ 4.2 ที่เวอร์ชัน 3 แก้ไปเฉพาะ 11 จุดที่ระบุไว้ ไม่ได้ไล่ทั้ง repo
   แก้เพิ่มใน `dragonborn.md`, `origin-feats.md` 2 จุด, `bard.md` 2 จุด และ `06-dm/05-adventure-design.md`
8. **markup ที่พังจากสคริปต์เวอร์ชันก่อน** ได้แก่ ตัวหนาว่างเปล่า `****` 9 จุด
   ซึ่งที่จริงคือเลขอันดับ 1 ถึง 3 ที่หายไปจากรายการจัดอันดับใน 3 ไฟล์ (เลข 4 ถึง 6 ยังอยู่ครบ)
   และ `****ชื่อสเปล****` อีก 2 จุด เขียนใหม่พร้อมเติมคำว่า "อันดับ N คือ" ตามกฎข้อ 4.2
9. **เครื่องหมายลบแบบ Unicode U+2212 จำนวน 85 จุด ใน 21 ไฟล์** ปนกับ hyphen ปกติ
   screen reader อ่านสองตัวนี้ไม่เหมือนกัน เปลี่ยนเป็น hyphen ให้หมด และเพิ่มเข้าสคริปต์ตรวจแล้ว

งานเพิ่มเติมที่ทำไปด้วย

- เติมหัวข้อ "สรุป ... ในหนึ่งบรรทัด" ให้ 8 ไฟล์ที่ขาด ตามกฎข้อ 6
  และเติมลิงก์กลับท้ายไฟล์ให้อีก 3 ไฟล์
- **แก้ค่าที่ผิดกฎ** ใน `04-classes/bard.md` หัวข้อ Jack of All Trades
  ของเดิมเขียนว่าเลเวล 5 ถึง 12 บวก 1 และเลเวล 13 ถึง 16 บวก 2 ซึ่งไม่ตรงกับ Proficiency Bonus
  ค่าที่ถูกคือครึ่งหนึ่งของ Proficiency Bonus ปัดลง ได้แก่ เลเวล 2 ถึง 8 บวก 1,
  เลเวล 9 ถึง 16 บวก 2 และเลเวล 17 ถึง 20 บวก 3

## ผลตรวจหลังทำเสร็จ

รันสคริปต์ตรวจชุดใหม่ในหัวข้อ "คำสั่งตรวจ" ซึ่งตรวจ 27 เรื่อง ได้ 0 ทุกเรื่อง
79 ไฟล์ ประมาณ 36,450 บรรทัด เหลือตาราง 23 ตาราง ทุกตารางไม่เกิน 4 คอลัมน์
ไม่มีตัวหนาในช่อง และมีย่อหน้านำหน้าครบ

---

# งานเวอร์ชัน 3 เก็บงานที่เวอร์ชัน 2 ตกหล่น (2026-08-14)

สถานะ **เสร็จแล้ว** ทำ 6 เรื่อง

1. **คำว่า "ตาราง" ที่ไม่มีตารางจริง** แก้ 80 จุด ตามกฎข้อ 4.1
   ตอนนี้เหลือคำว่า "ตาราง" 42 จุด ทุกจุดอยู่ติดกับตารางจริงภายใน 8 บรรทัด
2. **รายการที่ชื่อคอลัมน์หายไปตอนแปลงด้วยสคริปต์** แก้ตามกฎข้อ 4.2 ได้แก่
   รายการเทียบ 12 คลาสใน `04-classes/00-classes-overview.md`,
   Spell Slot ของ Eldritch Knight ใน `fighter.md`, Action Surge ใน `fighter.md`,
   ดาเมจ Divine Smite และคลัง Lay On Hands ใน `paladin.md`, Aura of Protection ใน `paladin.md`,
   Unarmored Movement ใน `monk.md`, Mystic Arcanum ใน `warlock.md`,
   Dwarven Toughness ใน `dwarf.md`, Feat Tough ใน `origin-feats.md`,
   งบ XP และ XP ตาม CR ใน `06-dm/02-building-encounters.md`
   และ Circle Spells ของ Druid ที่เดิมยัด 4 ภูมิประเทศไว้บรรทัดเดียว ตอนนี้แตกเป็นหัวข้อย่อยรายภูมิประเทศแล้ว
   บวกกับรายชื่อ Subclass Spell อีก 90 บรรทัดที่เติมคำว่า "เลเวล N ได้" นำหน้า
3. **bullet `•` ตกค้าง 221 จุดใน 24 ไฟล์** สคริปต์ตรวจเดิมจับไม่ได้เพราะตรวจแค่ `·` แก้หมดแล้ว และเพิ่มเข้าสคริปต์ตรวจแล้ว
4. **ลิงก์ที่ไปไม่ถึงไหน** ลบลิงก์สมอภาษาไทย `fighter.md#3--eldritch-knight--...` ใน `rogue.md` เปลี่ยนเป็นอ้างชื่อหัวข้อแทน ตอนนี้ลิงก์เสีย 0 และลิงก์สมอ 0
5. **คำแปลที่ขาดบริบท** แก้ 2 แบบ
   - หัวข้อที่เรียงอังกฤษก่อนไทย 112 หัวข้อ เปลี่ยนเป็นรูปแบบ ไทย (English) เช่น `## Alert ตื่นตัว` เป็น `## ตื่นตัว (Alert)`
   - ชื่อเผ่าที่โผล่เป็นภาษาอังกฤษล้วนโดยไม่มีคำไทยเลยในไฟล์นั้น เช่น Dragonborn เติมคำไทยให้ที่การกล่าวถึงครั้งแรกของแต่ละไฟล์ รวม 39 ไฟล์
6. **เติมข้อมูล Weapon Mastery** ใน `01-basics/08-equipment.md` เพิ่มกฎการใช้ 7 ข้อ,
   คุณสมบัติทั้ง 8 แบบพร้อมรายชื่ออาวุธที่มีคุณสมบัตินั้นครบทั้ง 38 ชนิด,
   จำนวนที่แต่ละคลาสได้และเพิ่มตอนเลเวลไหน, วิธีเลือกตามแนวทางการเล่น, ชุดที่แนะนำสำหรับมือใหม่
   และข้อผิดพลาดที่มือใหม่ทำบ่อย 6 ข้อ ส่วน `01-basics/05-combat.md` เพิ่มสรุปสั้นทั้ง 8 แบบ

งานเพิ่มเติมที่ทำไปด้วย คือแก้หัวข้อที่ข้ามระดับ h1 ไป h3 จำนวน 6 จุดใน `10-spells/`
โดยลด `# สเปลระดับ N` กลางไฟล์เป็น `##` ให้ตรงกับหัวข้อพี่น้องที่เป็น `##` อยู่แล้ว

ผลตรวจหลังทำเสร็จ ทุกอย่างเป็น 0 คือ สัญลักษณ์ต้องห้าม, ตารางเกิน 4 คอลัมน์, อีโมจิ,
ลิงก์เสีย, ลิงก์สมอ, ตัวหนาไม่ครบคู่, หัวข้อข้ามระดับ และตารางที่ไม่มีย่อหน้านำหน้า
รวม 80 ไฟล์ ประมาณ 36,900 บรรทัด

---

# แผนงานเวอร์ชัน 2 ปรับเอกสารให้รองรับ screen reader

เริ่ม 2026-08-09 สถานะ **เสร็จครบทุกเฟสแล้ว (อัปเดต 2026-08-09)**

ผลตรวจครั้งสุดท้ายทั้ง repo
- ไม่เหลือ em dash, middot, ตัวคูณ, ลูกศร, backtick, blockquote, `<br>`, `[C]`, `[R]` และอีโมจิ
- ตัวหนาครบคู่ทุกบรรทัดในทุกไฟล์
- เหลือตารางทั้งหมด 39 ตาราง กว้างสุด 4 คอลัมน์ ทุกตารางมีย่อหน้านำหน้า
- ลิงก์ภายในใช้ได้ทุกอัน 0 ลิงก์เสีย
- 80 ไฟล์ ประมาณ 36,600 บรรทัด
- สเปลทุกอันมีรายละเอียดเต็มแล้ว ไม่เหลืออันที่เขียนว่าให้ไปดูหนังสือต้นฉบับ
- ไม่เหลือ code fence เลย บล็อกโค้ดเดิม 79 บล็อกแปลงเป็นรายการข้อความ และแผนที่ ASCII 2 อันเขียนเป็นข้อความบอกลำดับห้องแทน

หมายเหตุ ไฟล์ class-lists สร้างด้วยสคริปต์ `gen_class_lists.py` ซึ่งดึงรายชื่อสเปลจากตารางในไฟล์ `04-classes/` แล้วดึงรายละเอียดเต็มจาก `10-spells/` ดังนั้น ตอนนี้ตารางรายชื่อเวทในไฟล์คลาสถูกลบไปแล้วในเฟส 4 ดังนั้น **รันสคริปต์นั้นซ้ำไม่ได้อีก** ถ้าต้องแก้ไฟล์ class-lists ให้แก้ที่ไฟล์โดยตรง

ไฟล์ต้นแบบของรูปแบบใหม่คือ `01-basics/01-what-is-dnd.md` ถ้าไม่แน่ใจว่าจะเขียนยังไง ให้เปิดไฟล์นั้นดูก่อน

## เป้าหมาย 3 ข้อ

1. แปลงตารางเป็นข้อความให้มากที่สุด เหลือตารางเฉพาะที่เป็นการเทียบจริงและไม่เกิน 4 คอลัมน์
2. ลดสัญลักษณ์ตกแต่งทั้งหมดตามกฎข้อ 4
3. ย้ายข้อมูลสเปลแบบเต็มมาอยู่ใกล้คลาส เพื่อให้คนที่กำลังเลือกคลาสเรียนรู้คลาสนั้นได้จบในที่เดียว

## การตัดสินใจที่ผู้ใช้เลือกไว้แล้ว (ห้ามเปลี่ยนเอง)

1. **ที่เก็บสเปลของคลาส** สร้างไฟล์แยกต่อคลาสที่ `10-spells/class-lists/ชื่อคลาส.md`
   ไฟล์คลาสใน `04-classes/` ลิงก์ไปหา ไม่เอาเนื้อหาเต็มมาใส่ในไฟล์คลาส
   เหตุผล ไฟล์คลาสไม่บวมเกิน 1,000 บรรทัด และหาสเปลได้ในที่เดียวจริง
2. **นโยบายตาราง** เก็บเฉพาะตารางเทียบ 2 ถึง 4 คอลัมน์ ที่เหลือแปลงเป็นข้อความทั้งหมด
3. **โฟลเดอร์ 10-spells เดิม** เก็บไว้ และปรับให้อ่านง่ายด้วย ใช้เป็นดัชนีกลางเรียงตามระดับสเปล
   ยอมรับว่าข้อมูลซ้ำกับ class-lists แต่คนละวิธีค้นหา

## ขนาดงานที่สำรวจแล้ว

- ตารางทั้ง repo 997 ตาราง ในจำนวนนี้กว้างตั้งแต่ 5 คอลัมน์ขึ้นไป 75 ตาราง
- แถวรายชื่อสเปลในไฟล์คลาส รวม 1,064 แถว แยกเป็น Wizard 241, Sorcerer 162, Bard 152, Druid 142, Cleric 126, Warlock 106, Ranger 72, Paladin 63
- ไฟล์ที่ต้องแตะทั้งหมด 79 ไฟล์ (เดิม 71 ไฟล์ บวกไฟล์ใหม่ 9 ไฟล์ ลบ CLAUDE.md ที่ทำเสร็จแล้ว)

## วิธีทำงานเพื่อกัน context หายและกัน quota หมด

1. ทำทีละ 2 ถึง 4 ไฟล์ต่อหนึ่งรอบ **ห้ามเปิดหลายไฟล์พร้อมกันทั้งเฟส**
2. **ทำไฟล์ไหนเสร็จ ให้กลับมาติ๊ก `[x]` ในเช็กลิสต์ข้างล่างทันที** ก่อนขึ้นไฟล์ถัดไป
3. แก้ด้วย Edit ทีละจุด ไม่ใช้สคริปต์ regex แก้ทั้ง repo (ดูข้อควรระวังข้อ 5 ท้ายไฟล์)
4. จบทุกเฟส ให้รันคำสั่งตรวจในหัวข้อ "คำสั่งตรวจ" แล้วบันทึกผลไว้
5. ถ้า context ถูกตัด ให้อ่านไฟล์นี้ก่อนเสมอ แล้วทำต่อจากไฟล์แรกที่ยังเป็น `[ ]`

## เฟส 1 พื้นฐาน 11 ไฟล์

เริ่มที่นี่เพราะเป็นไฟล์ที่คนอ่านมากที่สุด และใช้เป็นต้นแบบรูปแบบให้เฟสอื่น

- [x] `01-basics/01-what-is-dnd.md` เสร็จแล้ว เหลือตารางเทียบ VTT ไว้ 1 ตาราง 3 คอลัมน์ พร้อมย่อหน้าสรุปนำหน้า ใช้เป็นไฟล์ต้นแบบของรูปแบบใหม่
- [x] `01-basics/02-dice-and-rolls.md` เสร็จแล้ว ไม่เหลือตารางเลย
- [x] `01-basics/03-ability-scores.md` เสร็จแล้ว สกิล 18 อย่างจัดกลุ่มตาม Ability เป็นหัวข้อย่อย เหลือตารางเทียบค่าหลัก 12 คลาสไว้ 1 ตาราง 3 คอลัมน์
- [x] `01-basics/04-character-creation.md` เสร็จแล้ว ไม่เหลือตารางเลย ไฟล์นี้เคยมีอักขระ keycap แบบอีโมจิในหัวข้อ (เลข 1 ถึง 8 ในกรอบ) ลบออกแล้ว สคริปต์ตรวจอีโมจิเดิมจับไม่ได้เพราะอยู่นอกช่วง codepoint ที่ตรวจ
- [x] `01-basics/05-combat.md` เสร็จแล้ว ไม่เหลือตารางเลย
- [x] `01-basics/06-conditions.md` เสร็จแล้ว ไม่เหลือตารางเลย
- [x] `01-basics/07-spellcasting-rules.md` เสร็จแล้ว ไม่เหลือตารางเลย ตาราง Spell Slot ของ Full Caster ที่กว้าง 10 คอลัมน์ 20 แถว แปลงเป็นแบบ ค หนึ่งบรรทัดต่อหนึ่งเลเวล ใช้เป็นต้นแบบให้ไฟล์คลาสในเฟส 4
- [x] `01-basics/08-equipment.md` เสร็จแล้ว ไม่เหลือตารางเลย ตารางอาวุธ 6 คอลัมน์แปลงเป็นหนึ่งบรรทัดต่อหนึ่งอาวุธ เรียงเป็น ชื่อ ราคา ดาเมจ น้ำหนัก คุณสมบัติ ความชำนาญ ใช้ลำดับนี้กับของทุกชิ้นในไฟล์อื่นด้วย
  หมายเหตุ ข้อมูลดาเมจของน้ำมันหนึ่งขวด (Oil flask) ในไฟล์เดิมเขียนว่า 1d5 ซึ่งไม่ใช่ลูกเต๋าที่มีจริง เปลี่ยนเป็นให้ผู้อ่านเช็คหนังสือต้นฉบับแทน
- [x] `01-basics/09-rest-and-leveling.md` เสร็จแล้ว เหลือตารางเทียบ Short Rest กับ Long Rest ไว้ 1 ตาราง 3 คอลัมน์
- [x] `01-basics/10-adventuring.md` เสร็จแล้ว เหลือตารางเทียบแหล่งแสงไว้ 1 ตาราง 4 คอลัมน์
- [x] `01-basics/11-multiclassing.md` เสร็จแล้ว ไม่เหลือตารางเลย

## เฟส 2 สเปลเรียงตามระดับ 7 ไฟล์

ทำก่อนเฟส 3 เพราะเฟส 3 ต้องใช้รูปแบบข้อความสเปลจากเฟสนี้

- [x] `10-spells/00-spells-overview.md` เสร็จแล้ว เขียนใหม่ทั้งไฟล์ด้วยมือ เหลือตารางสารบัญไว้ 1 ตาราง 3 คอลัมน์
- [x] `10-spells/01-cantrips.md` เสร็จแล้ว ไม่เหลือตารางเลย
- [x] `10-spells/02-level-1.md` เสร็จแล้ว ไม่เหลือตารางเลย
- [x] `10-spells/03-level-2.md` เสร็จแล้ว ไม่เหลือตารางเลย
- [x] `10-spells/04-level-3.md` เสร็จแล้ว ไม่เหลือตารางเลย
- [x] `10-spells/05-level-4-5.md` เสร็จแล้ว ไม่เหลือตารางเลย
- [x] `10-spells/06-level-6-9.md` เสร็จแล้ว ไม่เหลือตารางเลย

## เฟส 3 ไฟล์เวทแยกตามคลาส 9 ไฟล์ใหม่

แต่ละไฟล์ประกอบด้วย

1. หัวเรื่องและลิงก์กลับไปไฟล์คลาสและสารบัญ
2. ย่อหน้าอธิบายว่าคลาสนี้ใช้เวทยังไง เตรียมสเปลหรือรู้สเปลตายตัว ใช้ค่าพลังตัวไหน
3. เวทเรียงตามระดับ ระดับ 0 ไปจนถึงระดับสูงสุดของคลาสนั้น เขียนแบบ ข ทุกอัน
4. หัวข้อ "เวทที่แนะนำสำหรับมือใหม่" แยกตามเลเวลตัวละคร
5. สรุปหนึ่งบรรทัดและลิงก์กลับ

**ที่มาของเนื้อหา** ใช้รายชื่อเวทเดิมจากไฟล์คลาสใน `04-classes/` เป็นตัวตั้ง แล้วเติมรายละเอียดจาก `10-spells/`
สเปลที่ยังไม่มีรายละเอียดใน `10-spells/` ให้เขียนเท่าที่รู้แน่ แล้วปิดท้ายว่า "รายละเอียดเต็มดูจากหนังสือต้นฉบับ" ตามกฎข้อ 8 ห้ามเดา

- [x] `10-spells/class-lists/00-class-lists-overview.md` เสร็จแล้ว เขียนด้วยมือ
- [x] `10-spells/class-lists/wizard.md` เสร็จแล้ว 167 สเปล (รายละเอียดเต็ม 101 ย่อ 66) 2,140 บรรทัด
- [x] `10-spells/class-lists/sorcerer.md` เสร็จแล้ว 117 สเปล (เต็ม 91 ย่อ 26)
- [x] `10-spells/class-lists/bard.md` เสร็จแล้ว 106 สเปล (เต็ม 70 ย่อ 36)
- [x] `10-spells/class-lists/druid.md` เสร็จแล้ว 103 สเปล (เต็ม 53 ย่อ 50)
- [x] `10-spells/class-lists/cleric.md` เสร็จแล้ว 87 สเปล (เต็ม 42 ย่อ 45)
- [x] `10-spells/class-lists/warlock.md` เสร็จแล้ว 66 สเปล (เต็ม 44 ย่อ 22) ส่วน Eldritch Invocations ยังอยู่ในไฟล์คลาส ไม่ได้ย้ายมา
- [x] `10-spells/class-lists/ranger.md` เสร็จแล้ว 61 สเปล (เต็ม 26 ย่อ 35)
- [x] `10-spells/class-lists/paladin.md` เสร็จแล้ว 52 สเปล (เต็ม 22 ย่อ 30)
- [x] อัปเดต `README.md` ให้มีสารบัญของ class-lists แล้ว และต่อลิงก์จาก `10-spells/00-spells-overview.md` ด้วย

## เฟส 4 ไฟล์คลาส 13 ไฟล์

งานในแต่ละไฟล์คลาส

1. แปลง Class Table เป็นแบบ ก
2. แปลงตารางเลเวล 1 ถึง 20 เป็นแบบ ค
3. แปลงตาราง Feature ทุกอันเป็นข้อความ
4. ตารางเทียบ 4 Subclass เก็บได้ถ้าไม่เกิน 4 คอลัมน์ พร้อมย่อหน้าสรุปนำหน้า
5. ลบตารางรายชื่อเวทออก แล้วแทนด้วยหัวข้อสั้น ๆ บวกลิงก์ไป `10-spells/class-lists/`
6. ลบ blockquote em dash middot backtick ทั้งไฟล์

- [x] `04-classes/00-classes-overview.md` เสร็จแล้ว
- [x] `04-classes/barbarian.md` เสร็จแล้ว
- [x] `04-classes/fighter.md` เสร็จแล้ว
- [x] `04-classes/rogue.md` เสร็จแล้ว
- [x] `04-classes/monk.md` เสร็จแล้ว
- [x] `04-classes/paladin.md` เสร็จแล้ว
- [x] `04-classes/ranger.md` เสร็จแล้ว
- [x] `04-classes/bard.md` เสร็จแล้ว
- [x] `04-classes/cleric.md` เสร็จแล้ว
- [x] `04-classes/druid.md` เสร็จแล้ว
- [x] `04-classes/sorcerer.md` เสร็จแล้ว
- [x] `04-classes/warlock.md` เสร็จแล้ว
- [x] `04-classes/wizard.md` เสร็จแล้ว

ทั้ง 13 ไฟล์ใช้สคริปต์ `convert_classes.py` แปลง ไม่เหลือตารางเลยสักไฟล์ ตรวจแล้วว่าตัวหนาครบคู่ทุกบรรทัด
หมวดรายการเวทในไฟล์คลาสสายเวท ถูกแทนด้วยหัวข้อสั้น ๆ บวกลิงก์ไป `10-spells/class-lists/` แล้ว

## เฟส 5 เผ่าพันธุ์ ภูมิหลัง และ Feat 17 ไฟล์

- [x] `02-species/00-species-overview.md` 7 ตาราง มีตารางเทียบ 10 เผ่าที่กว้าง ต้องตัด
- [x] `02-species/aasimar.md`
- [x] `02-species/dragonborn.md`
- [x] `02-species/dwarf.md`
- [x] `02-species/elf.md`
- [x] `02-species/gnome.md`
- [x] `02-species/goliath.md`
- [x] `02-species/halfling.md`
- [x] `02-species/human.md`
- [x] `02-species/orc.md`
- [x] `02-species/tiefling.md`
- [x] `03-backgrounds/00-backgrounds-overview.md` 20 ตาราง ภูมิหลัง 16 แบบให้เป็นหัวข้อย่อยแบบ ข
- [x] `03-backgrounds/origin-feats.md` 10 ตาราง
- [x] `05-feats/00-feats-overview.md`
- [x] `05-feats/general-feats.md`
- [x] `05-feats/fighting-styles.md`
- [x] `05-feats/epic-boons.md`

## เฟส 6 คู่มือ DM และมอนสเตอร์ 10 ไฟล์

Stat Block มอนสเตอร์เป็นตารางกว้างที่แย่ที่สุดสำหรับ screen reader มี 35 ตารางกว้างรวมกัน
รูปแบบใหม่ของ Stat Block ให้เป็นหัวข้อย่อยชื่อมอนสเตอร์ ตามด้วยรายการแบบ ก สำหรับค่าพื้นฐาน แล้วอธิบายการโจมตีเป็นข้อความ

- [x] `08-bestiary/00-bestiary-overview.md` เสร็จแล้ว Stat Block แปลงเป็นรายการข้อความหมดแล้ว
- [x] `08-bestiary/01-tier-1.md` เสร็จแล้ว Stat Block แปลงเป็นรายการข้อความหมดแล้ว
- [x] `08-bestiary/02-tier-2.md` เสร็จแล้ว Stat Block แปลงเป็นรายการข้อความหมดแล้ว
- [x] `08-bestiary/03-tier-3-4.md` เสร็จแล้ว Stat Block แปลงเป็นรายการข้อความหมดแล้ว
- [x] `06-dm/00-dm-overview.md`
- [x] `06-dm/01-running-the-game.md`
- [x] `06-dm/02-building-encounters.md`
- [x] `06-dm/03-monsters.md`
- [x] `06-dm/04-magic-items.md`
- [x] `06-dm/05-adventure-design.md`

## เฟส 7 ตัวละครสำเร็จรูป การผจญภัย และสารบัญ 12 ไฟล์

- [x] `07-pregen/00-pregen-overview.md`
- [x] `07-pregen/01-fighter-bran.md`
- [x] `07-pregen/02-barbarian-thokk.md`
- [x] `07-pregen/03-rogue-pip.md`
- [x] `07-pregen/04-cleric-elara.md`
- [x] `07-pregen/05-paladin-kaelen.md`
- [x] `07-pregen/06-ranger-lyra.md`
- [x] `07-pregen/07-bard-seraphine.md`
- [x] `07-pregen/08-wizard-fizzwick.md`
- [x] `09-adventures/00-adventures-overview.md`
- [x] `09-adventures/01-blackreach-mine.md`
- [x] `README.md` ปรับสารบัญและเพิ่มหมายเหตุว่าเอกสารชุดนี้ออกแบบให้ screen reader อ่านได้

## เฟส 8 ตรวจงานทั้งหมด

- [x] รันคำสั่งตรวจสัญลักษณ์ต้องห้าม ต้องไม่เหลือ
- [x] รันคำสั่งตรวจตารางกว้าง ต้องเหลือ 0 ตารางที่เกิน 4 คอลัมน์
- [x] รันคำสั่งตรวจลิงก์เสีย ต้องไม่เหลือ
- [x] รันคำสั่งตรวจอีโมจิ ต้องไม่เหลือ
- [x] อัปเดตหัวข้อ "สถานะ" ในไฟล์นี้

---

## คำสั่งตรวจ

ดูไฟล์ทั้งหมดและนับบรรทัด

```bash
find . -name "*.md" -not -path "./.git/*" | sort
wc -l $(find . -name "*.md" -not -path "./.git/*") | tail -1
```

**สำคัญ** สคริปต์ตรวจของเวอร์ชัน 2 และ 3 ตรวจแค่ 8 เรื่อง ทั้งที่กฎมีประมาณ 20 เรื่อง
ผลจึงออกมาเป็น 0 ทุกช่องทั้งที่ยังมีปัญหาค้างอยู่หลายพันจุด กฎข้อไหนไม่มีสคริปต์ตรวจ กฎข้อนั้นจะพังเสมอ
**ถ้าเพิ่มกฎใหม่ ต้องเพิ่มการตรวจในสคริปต์ข้างล่างนี้ทุกครั้ง**

สคริปต์เดียวตรวจครบทุกกฎ ผลลัพธ์ต้องเป็น 0 ทุกบรรทัด ยกเว้นบรรทัดสุดท้ายที่นับตารางที่เหลือ

```bash
python3 - <<'EOF'
import pathlib, re
from collections import Counter
files = [f for f in sorted(pathlib.Path('.').rglob('*.md'))
         if '.git' not in str(f) and f.name != 'CLAUDE.md']
bad = {'em dash':'—', 'en dash':'–', 'middot':'·', 'bullet':'•', 'multiply':'×',
       'not equal':'≠', 'arrow':'→', 'minus U+2212':'−', 'backtick':'`',
       'ถูก':'✓', 'ผิด':'✗'}
r = Counter()
for f in files:
    t = f.read_text(encoding='utf-8'); lines = t.split('\n')
    for k, v in bad.items():
        if v in t: r[k] += t.count(v)
    if '\n>' in t or t.startswith('>'): r['blockquote'] += 1
    if re.search(r'\[C\]|\[R\]', t): r['[C] หรือ [R]'] += 1
    if '****' in t: r['ตัวหนาว่างเปล่า ****'] += 1
    if '<br' in t: r['<br>'] += 1
    for ch in t:
        o = ord(ch)
        if 0x1F000<=o<=0x1FAFF or 0x2600<=o<=0x27BF or 0x2B00<=o<=0x2BFF or o==0x20E3:
            r['อีโมจิ'] += 1; break
    prev = 0
    for i, l in enumerate(lines, 1):
        if l.count('**') % 2: r['ตัวหนาไม่ครบคู่'] += 1
        m = re.match(r'^(#{1,6})\s', l)
        if m:
            lv = len(m.group(1))
            if prev and lv > prev + 1: r['หัวข้อข้ามระดับ'] += 1
            prev = lv
        if re.match(r'^\|(\s*:?-+:?\s*\|)+\s*$', l):
            r['ตาราง'] += 1
            if l.count('|') - 1 > 4: r['ตารางเกิน 4 คอลัมน์'] += 1
        elif l.strip().startswith('|') and '**' in l: r['ตัวหนาในช่องตาราง'] += 1
        if re.match(r'^#{1,6}\s', l) and re.search(r'\[[^\]]*\]\([^)]*\)', l):
            r['ลิงก์ในหัวข้อ'] += 1
        for mm in re.finditer(r'\[([^\]]+)\]\(([^)]+)\)', l):
            u = mm.group(2)
            if mm.group(1).strip().endswith('.md'): r['ข้อความลิงก์เป็นชื่อไฟล์'] += 1
            if mm.group(1).strip() in ('ที่นี่','คลิก','ลิงก์','นี่'): r['ข้อความลิงก์กำกวม'] += 1
            if not u.startswith('http') and '#' in u: r['ลิงก์สมอ'] += 1
            if u.endswith('.md') and not u.startswith('http') \
               and not (f.parent/u).resolve().exists(): r['ลิงก์เสีย'] += 1
        if re.match(r'^#{1,6}\s+\S', l) and not re.search(r'[ก-๙]', re.sub(r'^#{1,6}\s+','',l)):
            r['หัวข้ออังกฤษล้วน'] += 1
    ts = {i for i, l in enumerate(lines) if re.match(r'^\|(\s*:?-+:?\s*\|)+\s*$', l)}
    for i, l in enumerate(lines):
        if 'ตาราง' in l and not any(abs(i-x) <= 8 for x in ts):
            r['คำว่าตารางที่ไม่มีตารางจริง'] += 1
    hs = [re.sub(r'^#{1,6}\s+','',l).strip() for l in lines if re.match(r'^#{1,6}\s', l)]
    r['หัวข้อซ้ำตั้งแต่ 4 ครั้ง'] += sum(1 for k, v in Counter(hs).items() if v >= 4)
    if f.name != 'README.md' and 'ในหนึ่งบรรทัด' not in t: r['ไม่มีสรุปในหนึ่งบรรทัด'] += 1
print(f"ไฟล์ {len(files)} บรรทัด {sum(len(f.read_text(encoding='utf-8').split(chr(10))) for f in files)}")
for k in list(bad) + ['blockquote','[C] หรือ [R]','ตัวหนาว่างเปล่า ****','<br>','อีโมจิ',
        'ตัวหนาไม่ครบคู่','หัวข้อข้ามระดับ','ตารางเกิน 4 คอลัมน์','ตัวหนาในช่องตาราง',
        'ลิงก์ในหัวข้อ','ข้อความลิงก์เป็นชื่อไฟล์','ข้อความลิงก์กำกวม','ลิงก์สมอ','ลิงก์เสีย',
        'หัวข้ออังกฤษล้วน','คำว่าตารางที่ไม่มีตารางจริง','หัวข้อซ้ำตั้งแต่ 4 ครั้ง',
        'ไม่มีสรุปในหนึ่งบรรทัด']:
    print(f"  {k:32} {r[k]}")
print(f"  ตารางที่เหลือทั้งหมด {r['ตาราง']}")
EOF
```

หมายเหตุ การตรวจ "ตัวหนาเน้นอารมณ์" ตามกฎข้อ 4.3 อัตโนมัติทั้งหมดทำไม่ได้
เพราะต้องรู้ว่า span นั้นเป็นชื่อของสิ่งในเกมหรือไม่ วิธีที่ใช้ตอนทำเวอร์ชัน 4 คือ
เก็บรายชื่อไทยจากรูปแบบ ไทย (English) ทั่วทั้ง repo มาเป็นบัญชีขาว (ได้ 1,433 ชื่อ)
แล้วลบตัวหนาเฉพาะ span ที่ไม่อยู่ในบัญชีขาว ไม่ได้ขึ้นต้นด้วยตัวเลข และไม่ใช่คำแรกที่สั้นของ bullet
ถ้าจะทำซ้ำ ให้เขียนสคริปต์แบบเดียวกัน และ**ต้องทดสอบกับสำเนาก่อนเสมอ** ตามข้อควรระวังข้อ 4

---

## สถานะเนื้อหา (เวอร์ชัน 1 อัปเดต 2026-08-08)

71 ไฟล์ ประมาณ 22,600 บรรทัด ครบทุกอย่างที่ต้องใช้ในการเล่นจริง

- root 2 ไฟล์ คือ README.md และ CLAUDE.md
- 01-basics 11 ไฟล์ กฎพื้นฐานครบ
- 02-species 11 ไฟล์ overview บวก 10 เผ่า
- 03-backgrounds 2 ไฟล์ ภูมิหลัง 16 แบบ และ Origin Feats 10 อย่าง
- 04-classes 13 ไฟล์ overview บวก 12 คลาส
- 05-feats 4 ไฟล์
- 06-dm 6 ไฟล์
- 07-pregen 9 ไฟล์ overview บวกตัวละครเลเวล 1 จำนวน 8 ตัว
- 08-bestiary 4 ไฟล์ มอนสเตอร์ 33 ตัว
- 09-adventures 2 ไฟล์ การผจญภัย 1 เรื่อง เลเวล 1 ถึง 3
- 10-spells 7 ไฟล์ ประมาณ 140 สเปล

---

## งานที่อาจทำต่อได้ (ยังไม่ได้ทำ)

- สเปลที่เหลือใน PHB โฟลเดอร์ 10-spells มีประมาณ 140 สเปลที่ใช้บ่อยที่สุด ยังไม่ครบทุกอันในหนังสือ
- มอนสเตอร์เพิ่มเติม โฟลเดอร์ 08-bestiary มี 33 ตัว ยังไม่ครบ Monster Manual
- การผจญภัยเรื่องที่ 2 ขึ้นไป ตอนนี้มี 1 เรื่อง เลเวล 1 ถึง 3 ยังไม่มีเรื่องต่อสำหรับเลเวล 4 ขึ้นไป
- ตัวละครสำเร็จรูปเลเวลสูง ตอนนี้มีแค่เลเวล 1 จำนวน 8 ตัว
- ของวิเศษแบบมี Stat Block เต็ม ไฟล์ 06-dm/04-magic-items.md เป็นตารางสรุป ยังไม่มีรายละเอียดเต็มแต่ละชิ้น

---

## สิ่งที่ต้องระวัง

1. อัปเดต README.md ทุกครั้งที่เพิ่มไฟล์ เพราะตารางสารบัญอยู่ที่นั่น
2. ตรวจลิงก์สัมพัทธ์ ไฟล์ในโฟลเดอร์ย่อยใช้ `../README.md` ส่วนไฟล์ใน `10-spells/class-lists/` ต้องใช้ `../../README.md`
3. อย่าเปลี่ยนรูปแบบกลางคัน ทุกไฟล์ต้องใช้รูปแบบเดียวกันหมด ระหว่างที่แผนเวอร์ชัน 2 ยังไม่เสร็จ จะมีไฟล์สองแบบปนกันชั่วคราว ให้ดูเช็กลิสต์ว่าไฟล์ไหนแปลงแล้ว
4. ห้ามแก้เอกสารด้วยสคริปต์ regex ทั้ง repo โดยไม่ทดสอบกับสำเนาก่อน เคยทำ blockquote และตัวหนาซ้อนพังมาแล้ว ให้ copy ไปทดสอบใน temp directory เสมอ และระวัง `\s` ใน regex ที่ข้ามบรรทัดได้
5. เอกสารเป็นสรุปเพื่อการเรียนรู้ ไม่ใช่การแปลหนังสือ disclaimer ลิขสิทธิ์อยู่ท้าย README.md
